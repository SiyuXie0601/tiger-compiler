 letExp(
  decList(
   varDec(c,
    int,
    intExp(11),
    TRUE),
   decList(
    varDec(str,
     string,
     stringExp(hello world),
     TRUE),
    decList(
     varDec(i,
      int,
      intExp(0),
      TRUE),
     decList(
      typeDec(
       nametyList(
        namety(intArray,
         arrayTy(int)),
        nametyList())),
      decList(
       varDec(row,
        arrayExp(intArray,
         varExp(
          simpleVar(c)),
         intExp(0)),
        TRUE),
       decList()))))),
  seqExp(
   expList(
    assignExp(
     simpleVar(c),
     opExp(
      PLUS,
      varExp(
       simpleVar(i)),
      intExp(1))),
    expList(
     whileExp(
      opExp(
       EQUAL,
       varExp(
        simpleVar(c)),
       intExp(1)),
      assignExp(
       simpleVar(c),
       opExp(
        PLUS,
        varExp(
         simpleVar(c)),
        intExp(1))))
,
     expList(
      forExp(i,
       intExp(0),
       intExp(10),
       assignExp(
        simpleVar(c),
        opExp(
         PLUS,
         varExp(
          simpleVar(c)),
         intExp(1))),
       TRUE),
      expList(
       iffExp(
        iffExp(
         opExp(
          EQUAL,
          varExp(
           simpleVar(i)),
          intExp(0)),
         intExp(1),
         opExp(
          EQUAL,
          varExp(
           simpleVar(i)),
          intExp(1))),
        assignExp(
         simpleVar(i),
         intExp(1)),
        assignExp(
         simpleVar(i),
         opExp(
          MINUS,
          intExp(0),
          intExp(1)))),
       expList()))))))